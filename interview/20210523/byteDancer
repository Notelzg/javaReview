# 字节一面
## tcp 协议，
tcp 为啥3次捂手
tcp作为可靠性协议，
Client->Server 发起创建请求
Server->Client 服务端应答，客户端确认好了，但是服务端
Client->Server Ack 服务端收到确认，才知道客户端收到了信息
服务端和客户端都收到了Ack消息，才能互相确认都准备好了，才建立连接。
4次分手。
tcp是双工通道，读和写是两个互不影响的，所以需要分别确认
通道是否关闭，所以才需要4次挥手，少一次，某一端就缺少一个通道的关闭确认。
## 聚簇索引和非聚餐索引
聚簇索引其索引和数据共同存放，索引key就是主键，叶子节点就是数据，逻辑和物理结构保持一致。
逻辑上是B+树，物理上也是B+数据。
聚簇索引又称辅助索引，其数据节点是主键，需要两次才能找到数据。
## 多态
继承和接口实现，静态是重载参数不一样，编译时确定
动态是根据实现实例的不同，编译时确定调用哪个实例的方法。
## 单利的实现
静态成员变量
锁来实现
## 最小子串
